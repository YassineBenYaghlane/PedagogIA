repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.12
    hooks:
      - id: ruff
        args: [--fix]
        files: ^backend/
      - id: ruff-format
        files: ^backend/

  # TODO: uncomment after App.jsx refactor (see issue #4)
  # - repo: local
  #   hooks:
  #     - id: eslint
  #       name: eslint
  #       entry: npx eslint --fix
  #       language: system
  #       files: \.(js|jsx)$
  #       exclude: ^(dist|node_modules)/
